# Development / test dependencies for the feeds publisher.
#
# Install alongside the runtime requirements when running the test suite
# locally or in CI:
#
#     python -m pip install -r requirements.txt -r requirements-dev.txt
#
# The suite is fully offline: no test touches the network — fetch
# behavior is covered with fakes and committed fixtures.

-r requirements.txt

pytest>=7.0
pyyaml>=6.0   # test-only: parses .github/workflows/publish-feeds.yml
